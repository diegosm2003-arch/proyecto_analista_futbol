[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "futbol-analytics"
version = "0.1.0"
description = "Plataforma personal de analitica de futbol sobre LaLiga y las Big 5 ligas europeas"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

# Dependencias comunes a todos los servicios: configuracion y validacion.
dependencies = [
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
]

[project.optional-dependencies]
# Calculo puro: percentiles y clustering. Se testea sin base de datos.
analysis = [
    "pandas>=2.2",
    "numpy>=1.26",
    "scikit-learn>=1.5",
    "scipy>=1.13",
]
# Extraccion (FBref/Understat) y carga en PostgreSQL.
etl = [
    "soccerdata>=1.8",
    "pandas>=2.2",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
]
# Capa de servicio.
api = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
]
# Interfaz. mplsoccer es el estandar para pizza charts estilo FBref.
app = [
    "streamlit>=1.37",
    "requests>=2.32",
    "matplotlib>=3.9",
    "mplsoccer>=1.4",
    "pandas>=2.2",
]
dev = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "httpx>=0.27",
]

[tool.hatch.build.targets.wheel]
packages = ["src/futbol_analytics"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # bugbear
    "SIM", # simplificaciones
    "T20", # prohibe print(): las trazas van por logging
]

[tool.ruff.lint.per-file-ignores]
# Streamlit y los tests si pueden escribir a stdout.
"src/futbol_analytics/app/*" = ["T20"]
"tests/*" = ["T20"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
